class Breakpoints {
  Breakpoints._();

  /// Material compact / phone width.
  static const double compact = 600;

  /// Material medium / tablet split layouts.
  static const double medium = 840;

  /// Material expanded / large tablet.
  static const double expanded = 1200;
}
