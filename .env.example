# TransitOps GIS — environment template
# Copy values into your run configuration or pass via --dart-define.
# Do not put real secrets in source control.

APP_ENV=development
API_BASE_URL=https://api.example.invalid/v1
ARCGIS_API_KEY=
ARCGIS_PORTAL_URL=https://www.arcgis.com
